const links=[...document.querySelectorAll('.sidebar a')];const sections=links.map(x=>document.querySelector(x.getAttribute('href'))).filter(Boolean);const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){links.forEach(l=>l.classList.toggle('active',l.getAttribute('href')==='#'+e.target.id))}}),{rootMargin:'-20% 0px -65%'});sections.forEach(s=>observer.observe(s));
