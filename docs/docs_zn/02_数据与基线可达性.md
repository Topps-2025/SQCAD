# 数据与基线可达性

## 已落地资产

- LongMemEval-S：已下载、hash 固定并完成 BM25/dense/RRF 检索基线；
- LongMemEval Oracle：已下载，只作上界；
- LoCoMo：`locomo10.json` 已下载并固定 hash，完整 reader 尚未统一；
- Gate A：200 个证据包模板已生成，人工双标和裁决未完成。

## 尚未统一复现

GoodAI-LTM、MemoryAgentBench、Oblivion、FadeMem、SimpleMem 具备公开仓库或入口，但仍存在 runner、Python 版本、模型/API endpoint、许可证或论文实现对应关系问题。不能把仓库可访问写成已复现 SOTA。

## 外置数据库

所有大文件位于 `D:\Engineering\SQCAD\database`，manifest 记录路径、大小、来源、状态和 SHA-256。C 盘仓库只保存核心代码、测试和轻量文档。
