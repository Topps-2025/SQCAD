# 14-T2 严格化与 P4 探测下界历史数值佐证报告-20260813：reduction-separation 与旧阶诊断（superseded）

> **本报告为 `docs/自用/01-research-gap/研究逻辑与理论证明/16-T2严格reduction-separation与P4minimax探测下界-20260813.md` 的数值佐证**。代码 `src/sqcad/reduction_closure.py`（16 项新测试随全套通过）；结果 `results/reduction_closure.json`（配对恒等式与还原像：12 seeds 21–32；检测扫描：200 seeds 固定种子，确定性）；两处代码增量：`self_obscuring_ablation.py` 新增 `random_flip`（耦合硬币控制）与 `contextual_bandit_oracle`（禁止 $\phi$ 控制，16- §1.6）。

**状态：计算验证级 + 形式化证明配套（16-）**。本报告回答两条评审要求的数值侧：(T2) 任何忠实 feedback-preserving reduction 都无法把 self-obscuring 问题化约为普通 bandit/OPE——"contextual 编码一切"被逐位精确的 $\Theta(T)$ 像上后悔阻断；(P4) 固定样本 Bretagnolle--Huber 下界 $N^* = 2\log(1/(2\delta))/\operatorname{KL}$ 与后悔分解（证据获取 + 恢复等待 + 错误治理生命周期损失）被经验规则用于阶/常数诊断，并与 T1(b) 恢复上界比较。序贯停止规则不声称达到固定样本常数。

> **Scope correction（2026-08-21）：** 本报告保留为历史数值佐证。文中“与 T1(b) 恢复上界比较”“阶匹配”均不构成当前 matching claim；同类安全上下界以 Theorems 11–13 和 Certificate–Censoring Bridge B1–B4 为准。

---

## 1. 三组实验与协议

| 实验 | 对应定理 | 内容 |
|---|---|---|
| A 配对恒等式 | 16- 引理 4 | $\operatorname{regret}_K(\pi) + \operatorname{regret}_A(\pi) \equiv \tau p(T-n_{\mathrm{early}})$ 对任意策略逐点成立 |
| B 还原像 | 16- 定理 2 + §1.6 | 忠实化约像上的标准学习者精确 $\Theta(T)$ 失败；禁止 $\phi$ 控制成功 |
| C 检测扫描（历史诊断） | 16- 定理 3/4 + 推论 4 | $N^*(\delta)$ vs 经验探测数；后悔分解；旧 T1(b) 比较仅作诊断，不是当前安全 matching |

参数：$\tau=10$、$p=0.6$、$n_{\mathrm{early}}=50$、$T=2000$、$c_{\mathrm{probe}}=30$、$c_{\mathrm{restore}}=80$（与 13- 官方协议一致）；检测扫描 $\delta=0.05$（$z=1.96$）、$\sigma=1$、$\rho=1$、$q \in \{0.05, 0.2, 1.0\}$、$\tau$ 扫描 $\in \{0.25, 0.5, 1, 2, 5\}$（信号弱化扫描，覆盖 $N^* \in [0.06, 23.97]$）。

**精确基准**：$\tau p \cdot (T-n_{\mathrm{early}}) = 10 \times 0.6 \times 1950 = 11700.0$（每步恰一个世界动作错误，每步损失 $\pm 6.0$）。

## 2. 实验 A：配对后悔恒等式（引理 4）——逐点、任意策略、逐位精确

| 策略 | 配对后悔均值 | 期望恒等式 | 最大逐 seed 偏差 | 精确 |
|---|---|---|---|---|
| watchful_no_restore | 11700.0 | 11700.0 | 0.0 | ✅ |
| association_commit | 11700.0 | 11700.0 | 0.0 | ✅ |
| watchful_restore | 11700.0 | 11700.0 | 0.0 | ✅ |
| **random_flip** | 11700.0 | 11700.0 | 0.0 | ✅ |

（12 seeds 21–32，逐 seed 校验；含 committed、watchful、以及**最大适应性**的随机翻转策略。）

**random_flip 的设计**：翻转硬币用**世界无关**种子（`seed*104729+13`），使 K/A 两世界的状态轨迹**耦合**（16- 引理 3 的化约不变性在实现中的对应物）。在耦合下，任意时刻至多一个世界动作错误——即使策略每步以 0.5 概率翻转动作（1950 次任意重决策），配对和仍逐位等于 11700.0。

**读法**：这是定理 2 证明引擎的数值兑现——后悔配对恒等式对"任意"策略（包括完全自适应、不利用任何结构的策略）成立，且与策略的观测使用方式完全无关；线性失败是审查结构本身的内禀量，不是任何具体学习器的缺陷。

## 3. 实验 B：忠实化约像上的标准学习者（定理 2）

| 学习者 | 像上斜率 $K$ | 像上斜率 $A$ | 配对后悔和 |
|---|---|---|---|
| bandit_ucb（标准 bandit 学习者） | **5.8500** 精确 | 0.0000 | 11700.0 精确 |
| standard_ope（对数 OPE 学习者） | **5.8500** 精确 | 0.0000 | 11700.0 精确 |
| contextual_bandit（初始 kept，不审查） | 0.0278 | 0.0190 | 93.5 |
| **contextual_bandit_oracle**（禁止 $\phi$ 控制） | **0.0000** | 0.0000 | 0.0 |

**标准学习者在像上精确 $\Theta(T)$ 失败**：在忠实 feedback-preserving reduction（$\phi$ 为观测恒等，16- 定义 1）的像上，两个代表性标准学习者（UCB bandit、logistic/log OPE）产生逐位精确的 5.8500 斜率——与 13- 官方 W2 数值一致，即定理 2 的 $\Theta(T)$ 下界被兑现为精确常数，而非渐近预言。

**第三个控制（contextual_bandit）**：它初始状态为 kept，**从未被审查**——候选流持续暴露，它学习成功（slope ≈ 0.03）。这把失败的必要条件精确地钉在**行动依赖的审查**（提交归档 → 自生沉默）上，而非学习者能力不足：同一类学习者，不被审查就能学会。这与"任何问题都可以把所有信息编码进 contextual state"的审稿异议正交——在忠实化约（$\phi$ 世界无关）下，编码通道被阻断，而一旦允许不归档，学习问题本身就是普通的。

**禁止 $\phi$ 控制（contextual_bandit_oracle）**：图像上下文若能看到 latent 世界身份（$\phi$ 依赖世界、违反世界无关性），同一 UCB 学习者立刻成功（0.0000 精确，两世界）。这把不可能性精确归因于 $\phi$ 约束——正是 16- 定义 1(iv) 阻断"编码一切"的位置。

## 4. 实验 C1：固定样本检测界与序贯规则诊断

$N^*(\delta) = 2\log(1/(2\delta))/\operatorname{KL}$，$\operatorname{KL} = 2\tau^2/\sigma^2$（$N(\tau,\sigma^2)$ vs $N(-\tau,\sigma^2)$ 单观测 $\operatorname{KL}$）。这是恰好 $n$ 个独立成功探测的固定样本下界；CI-exclusion 是序贯规则，表中比值仅作阶/常数诊断：

| $\tau$ | $N^*$（固定样本） | 成功探测比值（$q=0.05$） | 成功探测比值（$q=0.2$） | 成功探测比值（$q=1.0$） |
|---|---|---|---|---|
| 0.25 | 36.84 | 1.50 | 1.44 | 1.55 |
| 0.5 | 9.21 | 1.81 | 1.67 | 1.69 |
| 1.0 | 2.30 | 2.23 | 2.17 | 2.14 |
| 2.0 | 0.58 | 3.94 | 3.89 | 3.93 |
| 5.0 | 0.09 | 21.71 | 21.71 | 21.71 |

（CI-exclusion 停止规则，$z=1.96 \leftrightarrow \delta=0.05$；200 seeds。）

- **诊断读法**：经验成功探测数与固定样本 $N^*$ 的比值只用于展示阈值与序贯 overshoot 的常数开销，不构成任意序贯策略的统一下界证明。
- **尝试率不改变成功观测数**：在独立 probe channel 中，$q$ 主要缩放等待时间；表格各列的成功探测数应近似一致，任何差异均属于 Monte Carlo 噪声。
- **强信号 regime**：当 $N^*<1$ 时，最小样本守卫主导，CI-exclusion 的比值会发散；这标记规则的保守性，不应解释为固定样本界被精确达到。

## 5. 实验 C2：后悔分解与阶匹配（定理 4 + 推论 4）

**分解验证**（每格点逐位一致）：$\mathbb{E}[\operatorname{Regret}_K] = c_{\mathrm{probe}} \cdot \mathbb{E}[\text{探测数}] + \tau p \cdot \mathbb{E}[\text{恢复等待步数}]$，即后悔 = 证据获取成本 + 恢复等待（错误治理期间的生命周期损失）；K 世界等待期每步损失 $\tau p = 6.0$。

**固定门槛子类的后悔诊断**：有限时域严格下界是截断 survival-sum 的生命周期项 $L_{\mathrm{life}}=\tau p\sum_{t=0}^{H-1}P\{\operatorname{Bin}(t,q\rho)<N^*\}$；只有在 $H\to\infty$ 且最终收集满 $N^*$ 个成功探测时，才得到 $L_\infty=(N^*/\rho)(\tau p/q+c_{\mathrm{probe}})$。CI-exclusion 的经验值只用于比较阶和常数。

| $\tau$ | $q=0.05$：$\mathbb{E}[\operatorname{regret}]/L$ | $q=0.2$：$\mathbb{E}[\operatorname{regret}]/L$ | $q=1.0$：$\mathbb{E}[\operatorname{regret}]/L$ |
|---|---|---|---|
| 0.25 | 1.50 | 1.44 | 1.55 |
| 0.5 | 1.80 | 1.67 | 1.69 |
| 1.0 | 2.23 | 2.12 | 2.14 |
| 2.0 | 3.86 | 3.84 | 3.93 |
| 5.0 | 21.10 | 21.56 | 21.71 |

绑定 regime 的比值约为 $1.4$–$2.2$；当 $N^*<1$ 时最小样本守卫导致比值上升。该表是固定门槛子类的阶/常数诊断，不是任意序贯策略的最优性证明。

**推论 4 阶匹配（保守诊断包络 $U_{\mathrm{cons}} = (\tau p + c_{\mathrm{probe}})/(q\rho) + c_{\mathrm{restore}}$ vs $L$）**：精确 Bernoulli 模型上界中的探测成本为 $c_{\mathrm{probe}}/\rho$；本表使用 $U_{\mathrm{cons}}$ 作为更保守的数值包络，不能把它读成精确期望成本。

| $\tau$ | $U/L$（$q=0.05$） | $U/L$（$q=0.2$） | $U/L$（$q=1.0$） |
|---|---|---|---|
| 0.25 | **0.14** | **0.12** | **0.09** |
| 0.5 | **0.58** | **0.47** | **0.35** |
| 1.0 | 2.32 | 1.88 | 1.40 |
| 2.0 | 9.26 | 7.53 | 5.60 |
| 5.0 | 57.91 | 47.05 | 34.98 |

- **$\tau \le 0.5$（$N^*$ 较大）**：$U/L$ 的数值比较仅适用于上述固定门槛子类；不能外推为任意序贯策略的严格支配结论。
- **$\tau = 1$（$N^* \approx 2.3$）**：$U/L$ 约为 $1.4$–$2.3$；该比较只针对固定探测门槛子类。
- **$N^* \to 0$（强信号 regime）**：$U/L$ 按 $1/N^*$ 发散，反映固定阈值下界的保守性；不能解读为序贯规则的常数最优性。

**$U_{\mathrm{cons}}$ 的紧度（主配置 $\tau=10$）**：13- 官方 12-seed 的 W3_K_watchful_restore 经验后悔 850.0（corr 141.7 步）vs $U_{\mathrm{cons}} = (6+30)/0.05 + 80 = 800$——比值 1.06；该数字是保守成本实例诊断，不是检测样本下界。

## 6. 判定表（16- §3 审稿回应的数值侧）

| 审稿要求 | 理论（16-） | 数值（本报告） |
|---|---|---|
| T2：严格 reduction 定义，阻断"contextual 编码一切" | 定义 1/2 + 定理 2 + 引理 3/4 | 配对恒等式逐位精确 11700.0（任意策略）；忠实像上标准学习者 5.8500 精确；禁止 $\phi$ 控制 0.0000 |
| P4：固定样本 $N \ge 2\log(1/(2\delta))/\operatorname{KL}$，高斯 $\Omega(\log(1/\delta)/\Delta^2)$ | 定理 3 | CI-exclusion 与固定样本界作阶/常数诊断；不外推为序贯常数最优 |
| P4：总后悔 = 证据获取 + 恢复等待 + 生命周期损失 | 定理 4 | 分解逐位一致；固定门槛子类的经验比值作阶/常数诊断 |
| P4：旧 T1(b) 同阶（已撤回） | 推论 4（历史诊断） | $U/L$ 随 $N^*$ 的诊断见表；不再声称恢复通道达到正确复杂度或支配任意序贯策略；当前安全 matching 见 Theorems 11–13 |

## 7. 声称纪律与校准边界

1. **逐点恒等式是证明引擎**（引理 4），不是结论本身——定理 2 的证明以它为前提；数值仅兑现引擎在实现中的成立，证明义务在 16-。
2. contextual_bandit 的"成功"不削弱定理 2：它是不采取归档行动的 uncensored 学习者，不在忠实化约的像上做归档治理决策；它钉死的是"失败必要条件是行动依赖的审查"。
3. 推论 4 的常数因子依赖检测阈值（$z \leftrightarrow \delta$）与通道常数（$q$、$\rho$、$c_{\mathrm{probe}}$、$c_{\mathrm{restore}}$）；强信号 regime 的 $U/L$ 发散由 $C(\delta) \propto 1/N^*$ 显式涵盖，**不是**阶不匹配——两界均 $\ll \Theta(T)$ 仍成立。
4. 与 13- 相同的边界：本批交付"archived-committed 决策类上的 self-obscuring 基础理论问题"（16- §4），**尚不能说已建立"Agent Memory 全部基础理论"**（校准边界见 16- §5 与 09-）。

## 8. 复现

```
PYTHONPATH=src python -m sqcad.reduction_closure --output results/reduction_closure.json
PYTHONPATH=src python -m pytest tests/test_reduction_closure.py -q
```

- 配对恒等式/还原像：12 seeds（21–32），W2 世界对，逐 seed 点值校验
- 检测扫描：200 seeds（seed0=3，确定性），CI-exclusion 停止规则（$z=1.96$，正侧排除 0），探测通道（$q$，$\rho=1$）每步尝试、成功暴露 $y \sim N(\pm\tau, \sigma^2)$
- 结果：`results/reduction_closure.json`（配对/还原像 + 全扫描格点）；代码增量：`self_obscuring_ablation.py` 的 `random_flip`（世界无关硬币）与 `contextual_bandit_oracle`（禁止 φ）
