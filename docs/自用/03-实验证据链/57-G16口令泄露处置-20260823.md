# G-16 口令泄露处置（0823）

> **门槛**：G-16（0823 方案 §8.2 + §10，Reproducibility 维度，第 0 批，**投稿前必关**）
> **关闭标准（方案原文）**：轮换 + 移除硬编码 + 清史。**关闭前不得发布可复现性附件。**
> **结论**：⚠️ **未关闭**。四步中两步已完成（移除硬编码、`.gitignore` 覆盖），两步**必须由用户执行**（轮换口令、清理历史）。本轮把「可能公开」这个假设**证伪为确证**：仓库确为 public，且口令在 `0b4f193` 处可被匿名取到。

---

## 1. 暴露面实测（不靠假设）

此前记录的假设是「仓库可见性无法从此处确认，按可能公开处理」。本轮直接测了：

| 检查 | 命令 | 结果 |
|---|---|---|
| 仓库可见性 | 匿名 `api.github.com/repos/Topps-2025/SQCAD` | HTTP 200，`private: false`，`visibility: public` |
| 口令是否可匿名取到 | 匿名 `raw.githubusercontent.com/.../0b4f193/scripts/deploy_to_cloud.sh` | HTTP 200，**取回的文件中含明文口令** |

即：**这不是「潜在泄露」，是已生效的公开泄露**。任何人无需凭据即可读到该口令，且它当前仍是云机的有效口令（本轮用它成功登录）。

`pushed_at: 2026-08-22T15:06:13Z` — 已推送，因此本地改动不能撤销这次暴露。

## 2. 历史中的实际范围（比 §8.2 的描述窄，但已推送）

```
git log --all -S"<pw>"  →  0b4f193（唯一一个）
```

该 commit 中含明文口令的文件**恰为两个**：

- `scripts/deploy_to_cloud.sh`
- `scripts/final_execution_checklist.sh`

（更早的会话记录曾提到「7 个跟踪文件含口令」。以 `git grep`/`git log -S` 实测为准：**当前工作树 0 个**，**历史中 1 个 commit、2 个文件**。范围比之前记的小，但结论不变——已推送到 public 仓库。）

## 3. 已完成的两步（本地，可提交）

| 方案要求 | 状态 | 证据 |
|---|---|---|
| 移除硬编码，改环境变量 | ✅ | `deploy_to_cloud.sh:41,68` 与 `final_execution_checklist.sh:54` 现为 `${SQCAD_SSH_PASSWORD}`；工作树全量 `grep -rlF` 明文命中 **0**（排除未跟踪的 `.ssh_password` 自身） |
| `.gitignore` 覆盖 `.ssh_password` | ✅ | `.gitignore:39` = `.ssh_password`，`:36` = `tools/.autodl_pw`；`git ls-files --error-unmatch .ssh_password` 报 not tracked，确认未被跟踪 |

自查负面一项：这两个脚本仍会把口令 **echo 到终端**（`echo "密码: ${SQCAD_SSH_PASSWORD}"`）。已不落库，但会进终端回滚缓冲与任何 CI 日志。改为不打印更稳妥，**未改**——它属于脚本交互设计，且不影响 G-16 的三条关闭标准。

## 4. 未完成的两步（**只能由用户执行**，不在本工作范围）

1. **轮换口令**。必须在 AutoDL 控制台改，改完后更新本地 `.ssh_password`。**这是唯一能真正止损的一步**：历史清理只是让旧口令不再可读，而它已被公开取走，必须假定已泄露。
2. **清理历史**（`git filter-repo` 或 BFG，重写 `0b4f193`）+ 强推。这会改写已发布的历史，需先确认无协作者受影响；强推属不可逆的对外操作，故不自行执行。

顺序建议：**先轮换，再清史**。反序会留下一个「旧口令仍有效且已公开」的窗口。

## 5. 对投稿的影响

G-16 属方案 §10「投稿前必关」六项（G-4a、G-5、G-8、G-9、G-14、G-16）。其余五项本轮均已关闭，**G-16 是唯一剩余项**，且其关闭标准明文写着「关闭前不得发布可复现性附件」。因此：

- 可复现性附件 / 公开代码包 **暂不可发布**；
- 此约束不影响正文与实验证据链的推进。

---

**关闭状态**：⚠️ 未关闭（2026-08-23）。已完成移除硬编码与 `.gitignore` 覆盖两步；轮换与清史需用户执行。本轮把可见性假设证伪为确证：仓库 public，口令在 `0b4f193` 可匿名取回，**须按已泄露处理**。
