# SQCAD 完整实验方案（23-）：理论证明 → 公开数据 → 自建数据集 → 消融

> 状态：进行中。本文是 SQCAD 全研究线的唯一总览实验方案，以四项实验基本要求为骨架——**对比实验、数据支撑、基线复现、评价指标**（外部评审反馈，见 0808 会议纪要 `docs/自用/02-历史草稿/07-杂项草稿与实验记录/07-0808会议-老师要求与后续执行清单-20260808.md`，下文简称「评审要求」）——并给出可复现命题、交付物与声称纪律。
>
> 行动准则（评审要求 §10）：先用小规模、可复现的预实验回答"数据能不能测、基线能不能比、指标能不能看出差异"；只有这三点成立后，再扩大实验。

> 阅读导航（2026-08-17 更新）：汇报与答辩前，先读[21- 完整实验链路与文档导航](../03-实验证据链/21-教师汇报完整实验链路与文档导航-20260817.md)，再以本文作为完整方案和实验矩阵入口；已完成的公开集结果见[19-AutoDL 公开集复现报告](../03-实验证据链/19-AutoDL公开集复现与Candidate-Guard调整报告-20260816.md)，自建集实测与审计见[20-LifecycleBench 公允性审计与基线矩阵](../03-实验证据链/20-SQCAD-LifecycleBench公允性审计与基线矩阵报告-20260817.md)，数据/指标/基线三要素核查见[24- 0818 讨论](24-0818讨论-数据指标基线三要素核查-20260818.md)。全部 Markdown 的用途与进入论文的方式见[文档分类索引](02-论文主体草稿与证据文档分类-20260817.md)。

> 数学排版约定（2026-08-17 修订）：本文统一使用 `$...$` 表示行内公式、`$$...$$` 表示独立公式；数学变量不再以 Unicode 希腊字母或普通文本伪公式呈现。代码常量、文件名和策略名仍使用反引号，避免与数学符号混淆。

---

## 0. 论证总图

七环论证链，每环有独立的：**问题 → 指标（L1/L2/L3 分层）→ 基线（呼应环 2 理论基线）→ 验收判定 → 已得证据 / 待办**。

```
环1 Gap 可实验命题 ── 环2 形式化基础理论（必要性/充分性，T1/T2/P4）
     │                                      │
     ▼                                      ▼
环3 框架设计（E-Q-A 三层 + 资格授权证书）──► 环4 公开数据统一合同（外部效度）
     │                                      ▲
     ▼                                      │
环5 自建 SQCAD-LifecycleBench（内部效度：反事实 lifecycle）── 环6 消融（必要性）
                                                     ▼
                                              环7 统计协议与可复现性（贯穿）
```

- **环 1+2 输出理论基线**：T1(a)(b)(c) 的无恢复提交斜率 $\tau p(T-n_{\mathrm{early}})/T$、T2 配对恒等式、P4 minimax 探测下界。**这些数值是环 4/5 基线选择的依据**：公开/自建数据上的治理基线（recency/FIFO/LRU/decay/keep-all/blind/defer）必须覆盖环 2 中证明的失败模式。
- **环 3 输出框架**：Evidence–Qualification–Access 三层；资格证书 $\tau(i,s)=V_s^\pi(\operatorname{keep}_i)-V_s^\pi(\operatorname{archive}_i)$ 及其识别区间 $[L,U]$（`21-` 方案，`src/sqcad/qualification_access.py` 已实现）。
- **环 4 验证外部效度**（候选覆盖、预算、成本、QA F1）：`16-`/`17-`/`19-` 统一合同。
- **环 5 验证内部效度**（keep/archive 反事实生命周期价值）：`22-` 数据集方案 → 本批 MVP 构建完成（1380 episodes）。
- **环 6 验证各模块必要性**：评审要求 §5 八项消融 + `22-` §9 授权/探测基线 + overlay 消融。

**环 → 证据文档引用**（各环论证的可追溯来源；编号文件见 [`02-` 文档分类索引](02-论文主体草稿与证据文档分类-20260817.md)，最新数字以证据链报告为准；三要素核查见 `24-` 0818 讨论）：

| 环 | 论证引用（md 文档） |
|---|---|
| 环 1 Gap | `01-` Gap 证明实验报告、`02-` Gap 实验公平性审查、`10-` 最接近工作全文级核对、`18-` overlay 实验报告 |
| 环 2 形式理论 | `11-`/`12-`/`13-`/`14-` 机制证据链（T1/T2/P4 证明与验收）、`15-` 基线审计、`16-` 统一合同主表与显著性判定 |
| 环 3 框架设计 | `10-` §8 框架基线、`21-` 最小方案（资格证书识别区间 $[L,U]$） |
| 环 4 公开数据 | `16-` 统一合同主表、`17-` 闭源基线验证、`18-` overlay 治理目标实验、`19-` AutoDL 复现与 Guard 调整 |
| 环 5 自建数据 | `22-` 数据集构建方案、`20-` LB 公允性审计与基线矩阵、`19-`/`21-` 交接 |
| 环 6 消融 | `16-`（version gate 负项）、`18-`（fallback/probe 消融显著项）、`20-`（矩阵区分度与框架修改判定）、`21-` E1/E2 验收 |
| 环 7 统计 | `01-` 统一合同定义、`20-` paired bootstrap、§7.6 R1–R8 公允性防线 |
| 三要素核查 | `24-` 0818 讨论：数据—指标—基线核查（2026-08-18） |

---

## 1. 分层指标设计（不同维度，互不替代）

三层指标必须不同，因为三环证据回答的问题不同：

| 层 | 服务环节 | 指标集合 | 证明的维度 | 不可用于 |
|---|---|---|---|---|
| **L1 理论指标** | 环 1/2 | 提交斜率、max regret 下界、minimax 探测下界、识别集合分离、配对恒等式 | 必要条件/充分条件的存在性（结构因果） | 系统性能对比 |
| **L2 公开数据指标** | 环 4 | Hit、evidence recall、LoCoMo 官方 token-F1、persistent/exposure tokens、probe 数、候选覆盖、成本 Pareto | 治理策略在真实轨迹上的资源效率与覆盖外部效度 | 因果资格证明（无生命周期金标） |
| **L3 自建数据指标** | 环 5 | lifecycle_value_keep/archive、tau_keep_archive、oracle 一致率、false-commit / safe-commit / abstention precision、lifecycle regret、false-forgetting regret、harmful-retention regret、rare-bridge rescue、archive-induced false forgetting、probe action-change rate、restore precision、time-to-recovery、scope/version mismatch detection | keep/archive 持久动作的反事实生命周期价值与治理因果 | 直接外推到真实部署 |

### 1.1 指标强调级别（主 / 次 / 不强调）

每环主张只依赖其”主”指标；”次”指标仅作报告；”不强调”指标出现差距时如实报告（`16-` bm25 召回、`20-` memory_worth 等价行均照此执行）。级别在运行前固定，防止事后挑选有利指标（评审要求 §4.3；核查依据 `24-` §3.1）：

| 层 | 主（主张依赖） | 次（仅报告） | 不强调（差距如实报告） |
|---|---|---|---|
| L1 理论 | 下界成立性（存在性证明：$\Theta(T)$、$\log(1/\delta)/\operatorname{KL}$、配对恒等式） | 斜率常数（$\tau p$、$1.06\times$） | —（结构证明不用于系统性能对比） |
| L2 公开 | 治理策略资源效率与覆盖：成本 Pareto、Guard 修复、probe/fallback 通道显著项 | Hit/Recall 明细、probe 数 | bm25 静态召回排名（检索质量差距，非本框架主张） |
| L3 自建 | false-commit / safe-commit / abstention 精度、lifecycle regret | 探测量、恢复延迟、time-to-recovery | 与 oracle 的绝对距离（oracle 是上界，不是可达目标） |

交叉约束（评审要求 §4.3”主/次/效率指标提前固定”）：L3 的 $\tau$ 用与 L2 同一成本合同计算，冻结常量为 $\gamma=0.9$、$v_{\mathrm{task}}=10$、$c_{\mathrm{harm}}=20$、$c_{\mathrm{exposure}}=0.05$、$c_{\mathrm{probe}}=1$、$c_{\mathrm{storage}}=0.01$、$\tau_{\mathrm{tol}}=0.5$（实现名见 `frozen.py`）。这保证”token 减少不以任务效用下降为代价”可在一个合同中验证（`22-` §9）。

---

## 2. 分层基线设计（呼应关系）

评审要求 §2.2 要求逐基线回答：来源论文、解决的问题、代码/数据可达性、proxy 标注、能否公平比较。三层基线及其与理论基线的呼应：

### 2.1 理论基线（环 1/2 使用）

| 基线 | 来源/论文 | 对应理论问题 | 状态 |
|---|---|---|---|
| W0 去审查（无 persist） | 自有（`reduction_closure.py`） | T1：行动触发审查是否必要 | 已复现：watchful 0.0440 vs W2 精确 5.8500 |
| W2 无恢复提交策略 | 自有 | T1(a) 精确预测 $\tau p(T-n_{\mathrm{early}})/T=10\times0.6\times1950/2000$ | 已复现：**5.8500 精确**，12-seed 全一致 |
| W3 恢复平台（watchful_restore / uncertainty_triggered / cost_aware） | 自有 | 恢复路径是否打破下界 | 0.4250 / 0.2937 / 0.0000 |
| random_flip 世界无关耦合硬币 | 自有 | T2：$\phi$ 世界无关的忠实化约下 $\max\operatorname{Regret}\geq\tfrac{1}{2}\tau p(T-n_{\mathrm{early}})=\Theta(T)$ | 配对恒等式 $\operatorname{Regret}_K+\operatorname{Regret}_A\equiv\mathbf{11700.0}$ 逐点精确 |
| UCB / standard_OPE / contextual bandit（忠实像） | 自有（简化） | P4：探测下界 $N^\star=\log(1/\delta)/\operatorname{KL}$；$\mathbb{E}[\operatorname{Regret}]/L\in[2.2,3.4]$ | 已复现；$U=800\approx850.2$（W3 经验，1.06$\times$） |

### 2.2 公开数据基线（环 4 使用；呼应环 2 的“治理策略”类基线）

基线不能只列 BM25、简单规则与 Oblivion。为避免“弱基线比较”，环 4 固定覆盖五类对照：检索、无治理/时间衰减、差异衰减、结果反馈、决策充分性。每一行必须标明是**官方端到端复现**、**官方代码级验证**、**行为代理**或**尚不可迁移**；只有前两类可作为“实现复现”表述，代理行只能比较对应的决策信号。

| 基线 | 核心信号 / 对照问题 | 当前证据状态 | 在统一合同中的角色与下一步 |
|---|---|---|---|
| BM25 | 查询—证据词法相关性 | 本地 + AutoDL 完整运行 | 检索覆盖上界与存储成本对照（74,092 vs 1,619 tokens，45$\times$） |
| dense / RRF | 语义检索、混合检索 | 权重不可得；`tools/precompute_dense.py` 已备好 | 必须在可用 GPU/权重环境补跑；此前一律标为 `skipped`，不以 BM25 代替 |
| keep-all、FIFO、LRU、recency、frequency decay | 无治理或时间/频率治理 | 本地规则实现 | 治理成本上界及 T1/T2 的普通衰减对照 |
| **FadeMem** | 相关性、访问频率与时间模式的差异衰减 | 官方 Agent-Memory 实现未核实；同名视频仓库已因身份不符排除；已有 `fademem` **行为代理** | 作为差异衰减强基线。先复核官方实现；未获证前只报告代理公式，绝不称“复现 FadeMem” |
| **Oblivion** | 基于 utility / access-frequency 的 decay activation | 官方 decay 数学已逐字验证；完整系统受 Python 3.12、端点与依赖阻塞 | 作为可验证的 decay-core 行；完整端到端仍是 `not reproduced` |
| **Memory Worth** | success/failure 共现形成的 trust / suppression | 官方实现未核实；已有 `memory_worth` **estimand-faithful 行为代理** | 作为结果反馈/关联治理强基线；不能把观测成功率误称为持久动作因果价值 |
| **DeMem** | 保留影响下游决策的区分、压缩中性信息 | 官方实现未核实；已有 `demem` **decision-distinction 行为代理** | 作为决策充分性强基线；完整压缩 decoder 不可迁移，代理仅比较其区分性准则 |
| SimpleMem | 写时 salience 与检索 gating | 官方读取器 + 词法评分层已验证；LLM 压缩/嵌入/生成未复现 | 官方代码级验证行；不可当作完整系统结果 |
| ReMe | 经验驱动的程序记忆 refine | 当前共享文本流缺少等价的“经验—执行—验证—精炼”闭环 | 作为相关工作和 Phase B 过程记忆对照登记；在构造可比 agent loop 前 `not transportable`，不填伪数字 |
| 原 SQCAD（v0.1） | 资格 + 竞争访问 | 本地 + AutoDL 完整运行 | 主消融对照 |
| Guard-1/2/4（Candidate Guard） | 一次性证据覆盖保底 | 本地 + AutoDL 完整运行 | Guard-1 是主配置；不是另一种长期治理基线 |

**核心基线执行门**：FadeMem、Memory Worth、DeMem 三个命名代理必须在同一 frozen stream、reader、budget、evaluator、seed 与成本合同下各跑一行，并以“代理”单独分栏；若后续得到可验证官方实现，再新增“官方复现”行而不覆盖代理结果。SimpleMem 与 Oblivion 保留已验证层和未验证层的拆分。ReMe 只有在 Phase B 提供相同的程序执行回路后才进入数值主表。

### 2.3 自建数据基线（环 5 使用；直接实例化环 2 的授权/探测分类学）

| 基线 | 对应 `22-` §9 分组 | 呼应理论 |
|---|---|---|
| keep-all / blind commit | 生命周期基线 / 授权基线 | T1：无资格提交的失败上界 |
| always defer | 授权基线 | P4：永不探测的保守成本 |
| FIFO / LRU / recency / frequency-decay | 生命周期基线 | T1/T2：普通衰减错误遗忘低频高价值记忆 |
| Memory Worth proxy | 结果反馈 / 关联治理 | 已在当前 MVP 矩阵运行；但现有生成特征下与 `storage12` 同动作，必须标为“等价行”，不能把它误读成独立胜负 |
| FadeMem proxy / Oblivion core-decay | 差异衰减 / activation decay | 按其可迁移核心信号分别实现；与 recency/frequency 分开报告，避免把普通衰减冒称方法复现 |
| DeMem proxy | 决策充分性 / 区分性压缩 | 只保留改变决策的区分；需报告压缩损失和错误保留，不能只报 token |
| SimpleMem verified-layer proxy | 写时 salience / retrieval gate | 只评价已验证的读取/评分层；LLM 压缩层不作无依据替代 |
| ReMe | 程序经验精炼 | 当前 L3 规则世界不具备等价执行闭环，列为 Phase B 的 `not transportable` 对照，不强行数值化 |
| CMI local-effect proxy | 局部干预效应 | 只建模 query-local effect，用 `local-vs-lifecycle conflict` 场景直接对测 |
| random probe / BM25-score probe / retrieval-score probe | 探测基线 | P4：非价值信息探测（对照 VOI probe 的价值优先排序） |
| blind point gate / forced commit | 授权基线 | 21-：识别集合跨动作边界仍提交（对应 forced-commit 消融） |
| SQCAD 消融族：no-qualification / no-censoring / no-restore / no-lineage-version-gate | 消融 | 环 6 必要性验证 |

**呼应关系表**（公开/自建基线须至少涵盖理论证明所用基线）：

```
环2 证明的失败模式            环4/5 中对应的基线类
──────────────────────     ─────────────────────
行动触发审查必要(T1)      ←  keep-all / blind commit / 原 SQCAD
恢复打破下界(T1 W3)       ←  restore 通道 + no-restore 消融
普通衰减错误遗忘(T2)      ←  recency / FIFO / LRU / frequency-decay
探测有下界(P4)            ←  random/BM25 probe 对照 VOI probe
识别集合不可分离(P4)      ←  forced commit / blind point gate
```

---

## 3. 环 1：Research Gap 与可实验命题

**问题**（评审要求 §2.1 固定表述，Intro/Method/Experiments 必须一致）：

> 在有限工作区预算下，普通关联信号可能把无关记忆一并强化，也可能因低频而遗忘稀有但高价值记忆。SQCAD 需验证：作用域限定的资格判断与竞争性访问投影，能否减少错误保留，同时保护低频高价值记忆，并保持未来任务效用。

**指标**：L1（Gap 命题的存在性判定：hitchhiker 共存率、低频高价值错误降权率、作用域/版本负迁移可观测性）。

**实验/证据**：
- `01-Gap证明实验报告-v2-20260812`（命题 A/B/C）+ `02-Gap实验公平性审查`：hitchhiker 关联强化、低频遗忘、作用域迁移三类现象在 LoCoMo/LME-S 上可观测；
- 评审要求 §3.3 快速验证（subset 预实验）已隐含完成：`18-` overlay 注入的 E1–E5 全部稳定复现；
- `10-最接近工作全文级核对`：与相关工作的因果定位。

**判定**：三类现象均可稳定复现 → Gap 成立，继续。**待办**：维持可实验表述与 Intro 一致性（跨论文写作时）。

## 4. 环 2：形式化基础理论（必要性/充分性证明）

**问题**：为什么该问题不能只用相关性、频率或普通衰减解决（必要性与充分性）。

**指标（L1）**：提交斜率、max regret 下界、探测下界、配对恒等式、识别集合分离。

**已得证明与数值**（证据链 `11-`/`12-`/`13-`/`14-`，代码 `reduction_closure.py`）：
- T1(a)(b)(c)（`15-`）：无恢复提交策略斜率为 **5.8500 精确**，即 $\tau p(T-n_{\mathrm{early}})/T$；W0 去审查为 0.0440；W3 恢复平台为 0.4250 / 0.2937 / 0.0000；W0/W1 reduction controls 有效；
- T2 严格化（`16-`，commit `481f18c`）：faithful feedback-preserving reduction；配对恒等式 $\operatorname{Regret}_K+\operatorname{Regret}_A\equiv\tau p(T-n_{\mathrm{early}})=\mathbf{11700.0}$ 对任意策略逐点精确；定理 2 为 $\max\operatorname{Regret}\geq\tfrac{1}{2}\tau p(T-n_{\mathrm{early}})=\Theta(T)$；数值：bandit_ucb / standard_ope 为 5.8500，oracle 为 0.0000，contextual_bandit 约为 0.03（**失败必要条件是行动触发的审查**）；
- P4 严格化（`16-`）：定理 3 为 $N^\star=\log(1/\delta)/\operatorname{KL}$，其中 $\operatorname{KL}=2\tau^2/\sigma^2$；定理 4 为后悔分解下界，且 $\mathbb{E}[\operatorname{Regret}]/L\in[2.2,3.4]$；$U=800\approx850.2$（W3 经验，1.06$\times$，上界紧）。

**验收判定**（`14-` §9 条件 1/2/3 全部严格满足）。**边界**：`16-` §5 校准——self-obscuring 基础理论已建立，**authorization certificate 形式化（P0）仍为遗留**（`21-` 方案 + `qualification_access.py` 已给出框架实现，形式化陈述待写）。

**待办**：证书形式化（$L>0$/$U<0$/crossing 的识别区间理论）；如需在理论层证明"普通衰减必然错过稀有 bridge"，可补一个引理。

## 5. 环 3：框架设计（三层 + 资格授权）

**设计**（`10-` §8 + `21-`）：
- Evidence 层：不可变证据日志（沉默语义：未暴露 $\ne$ 不存在）；
- Qualification 层：资格证书

$$
\tau(i,s)=V_s^\pi(\operatorname{keep}_i)-V_s^\pi(\operatorname{archive}_i),
\qquad \tau(i,s)\in[L,U].
$$

  - $L>0\Rightarrow\operatorname{keep}/\operatorname{restore}$；$U<0\Rightarrow\operatorname{archive}$；$L\leq0\leq U\Rightarrow\operatorname{defer}$ 或 VOI probe，其中

$$
\operatorname{VOI}=C_{\mathrm{defer}}-\left(C_{\mathrm{probe}}+R^\star(L',U')\right),
\qquad
R^\star(L,U)=\frac{U(-L)}{U-L}.
$$
  - 识别集合跨动作边界不允许"低 regret 随机 commit"（最小安全提交条件）；
- Access 层：资格投影（先移除被授权 archive 的对象）→ 排序投影（冻结 proposer 在资格兼容对象内选 top-$B$）。检索器只能决定"先看谁"，不能决定"谁长期留下"。

**实现**：`src/sqcad/qualification_access.py`（QualificationCertificate / decide_persistent_action / ProbeOption·probe_value / plan_access）+ 5 项结构测试（tests/test_qualification_access.py 全过）。

**指标（L1 派生）**：interval coverage、false-commit rate、safe-commit rate、action sign error（E1 验收）。

**待办**：E1 受控资格授权恢复实验（`21-` §3 E1：在 identification_recovery_experiment 上输出每 memory-scope 证书，treatment 随机化 keep/archive）。

## 6. 环 4：公开数据集统一合同实验（外部效度）

**统一合同**（`16-`/`19-`，`src/sqcad/public_unified_contract.py`）：所有方法共享 chronological stream、workspace budget、extractive reader、evaluator、成本记录；策略不读 gold；LongMemEval-S 用时间掩码（15175 msgs/869 needed），LoCoMo 用冻结官方 QA 顺序（1986 QA，官方 token-F1 原样调用）。

**指标（L2）**：Hit、evidence recall、LoCoMo 官方 token-F1、exposure/storage tokens、probe 数、候选覆盖；显著性用 studentized bootstrap CI（`bootstrap_ci.py`）。

**主表结果**：

| 方法 | LME-S Hit | LME-S Recall | LME-S Storage | LoCoMo F1 | LoCoMo E-rec | 备注 |
|---|---:|---:|---:|---:|---:|---|
| BM25 | 0.967 | 0.323 | 74,092 | 0.0454 | 0.5296 | 静态召回上界 |
| 原 SQCAD | 0.785 | 0.118 | 1,617 | 0.0344 | 0.1928 | 45$\times$ 存储压缩 |
| Guard-1（推荐） | 0.915 | 0.153 | 1,616 | **0.0455** | 0.3249 | ≥ BM25 F1，probe 2.0 |
| Guard-2 / Guard-4 | 0.929 / 0.950 | 0.179 / 0.224 | 1,616 / 1,615 | 0.0465 / 0.0475 | 0.3835 / 0.4480 | 成本—效果敏感性 |
| no_probe / no_restore | — | — | — | ~0.0161 / ~0.0163 | — | 探测/恢复通道非装饰 |

**显著性判定**（`16-`）：
- 对 bm25 静态召回**显著落后**（LME hit −0.188，CI 不跨 0；ku 旧版本召回 0.137 vs 0.325——temporal-consistency 假设对检索 oracle 不成立，recency 偏置所致）；
- **成本显著占优**（45$\times$ 存储压缩）；
- 机制消融显著项：probe/fallback/lineage/bundle；**version gate 在 rare_recall 上为负**（0.132 vs 0.160）——如实报告；
- Guard-1 在 LoCoMo 官方 F1 达到并略超 BM25（0.0455 vs 0.0454），recall 0.153 vs 0.118 改善为 bad case 修复（`19-` §3：支持证据未进入工作区）。

**闭源基线**（`17-`）：SimpleMem 官方读取器与合同 loader 精确一致；官方 inclusion-scoring hit 0.288（位于 bm25 0.572 与 sqcad 0.220 之间）；Oblivion decay 数学逐字执行断言通过（env/依赖阻塞精确记录）。**AutoDL GPU 全量复核**（`19-`）：RTX 4080 SUPER，SHA-256 一致，主结果与本机一致。

**半合成 chronological overlay**（`18-`，治理目标实验）：LoCoMo 注入 E1 版本更新 / E2 纠错 / E3 有害保留 / E4 稀有保护 / E5 self-obscuring 救援（全程序化标签，seed 20260814）。关键：E5 救援 0.206 vs 基线 0.000（显著）；fallback 消融 E2 harmful 0.96→0.30、E3 0.86→0.42（显著，`17-` 预测成立）；probe E1 hit +0.140 显著；E2 harmful 0.300 vs 0.540 方向有利但 $n=10$ 不显著。

**正确表述**（声称纪律）：治理策略最优 + 成本 Pareto + 检索质量差距；**不声称公开数据全面占优，不把公开集结果写成因果资格证明**。

**待办**：E3 验收闭环——dense 复现（GPU 环境直载 transformers，`tools/precompute_dense.py` 备好）；Guard-2/4 保留为敏感性；把 `21-` 的资格证书接入公开合同只做 budget sensitivity，不升级主张。

## 7. 环 5：自建 SQCAD-LifecycleBench（内部效度）

### 7.1 数据集（22- 方案 → 本批 MVP 构建完成）

**为什么需要**：LongMemEval-S/LoCoMo 没有同一持久动作在未来的对照轨迹，不能单独证明 keep/archive 的生命周期价值（22- §1）。

**构建结果**（`src/sqcad/lifecycle_bench/`，输出 `results/lifecycle_bench/`，本批 20260817 完成）：
- 6 家族 $\times$ 200（hitchhiker / rare_bridge / version_update / harmful_stale / self_obscuring / scope_mismatch）+ 3 controls $\times$ 50（stable_positive / stable_negative / neutral）+ 15 观测等价对（30 episodes，hitchhiker 槽 5 隐藏 needed e1→h1 翻转）＝ **1380 episodes**；
- 全部 14 个场景 bucket 的 oracle == designer label **严格一致**（含 controls 的 neutral 弃权），15/15 pairs flip 生效；
- 三层序列化：public.jsonl（无金标）/ policy_log.jsonl（reference_sqcad 双分支，无 needed/success/tau）/ hidden.jsonl（evaluator 独占标签）/ manifest.json（冻结合同：常量、seeds BASE=20260817 / PAIR=20260818 / SPLIT=20260819、split 表）；
- split：group 级（family, variant, entity）60/20/20 → train 818 / dev 354 / test 208；同一 pair 永不跨 split；
- 47 项数据合同测试全过（tests/test_lifecycle_bench_contract.py）：6 特征、branch fork、slot0 局部相同、oracle sign map、观测等价对、split hygiene、frozen manifest、policy log 无金标、probe 行为（crowding deny 仍计费 / rescue restore / NEGATIVE 禁 probe）。

**六特征落实**（22- §3）：持久动作分支（keep/archive 双 rollout，keep 字面语义：NEGATIVE 不自动驱逐，驱逐只由未来事件 requalify 触发）；真正 chronological future（槽 1–10，未来值不提前泄漏）；可执行 outcome（折扣效用按 frozen 合同计算）；同源反事实（共享未来流，slot0 位级相同）；隐藏可核验标签（needed ids / oracle 只在 hidden 层）；压力与不可识别世界（crowding、rescue 失败、观测等价翻转、version drift、harm 对称不可识别 → neutral）。

### 7.2 实验协议

- **Phase A（规则世界，本批完成）**：精确计算反事实价值、分支唯一差异检查、观测等价世界 paired bootstrap；
- **Phase B（端到端，待办）**：从规则世界抽取代表 episode，冻结 reader/agent/prompt/工具/模型版本，只切换 persistent memory state；工具环境 + 独立 evaluator 判定 outcome；
- **Phase C（公开集延续，已在进行）**：Guard-1、BM25/dense/RRF 与生命周期控制基线继续在 LME-S/LoCoMo 上跑覆盖/QA/成本/版本冲突/恢复，不写成因果资格证明。

### 7.3 指标（L3）

资格层：false-commit rate、safe-commit rate、abstention precision、interval coverage、scope/version mismatch detection、bundle unresolved rate。
访问层：lifecycle regret、future evidence recall、harmful exposure、rare-bridge rescue、archive-induced false forgetting、probe action-change rate、restore precision、time-to-recovery。
成本：persistent/exposure tokens、probe/restore 次数与成本、预注册成本合同下的 net utility。

### 7.4 基线（见 §2.3 表）

### 7.5 Go/No-Go 判据（22- §10）

1. SQCAD 比 blind commit 少 false commit；
2. SQCAD 比 always defer 有更多安全 commit；
3. VOI probe 比随机/BM25 probe 以更低成本更常改变正确动作；
4. 在相同或更低 lifecycle regret 下，storage/probe 成本可接受。

**当前完成与扩展待办**：1,380 episodes 的首轮矩阵和 paired bootstrap 已完成（见 `20-`）；其中 `memory_worth` 与 `storage12` 在现有特征下逐 episode 同动作，属于已发现的代理退化，而非两方法“恰好同分”。下一轮按 §2.2 的核心基线执行门补齐 FadeMem、Oblivion、DeMem、SimpleMem 的可迁移层，并单独报告代理/官方复现状态；ReMe 留至 Phase B 的可比 agent loop。随后才输出扩展 L3 主表与 bootstrap 显著性。

### 7.6 数据集修订与公允性防线（R1–R8，本批修订落地）

"数据集不是自娱自乐"的四通道判定：**真值可独立检验 / 失败可以发生 / 泛化可被挑战 / 评价不可被猜**。每条防线有自动化工具、预注册判据与审计输出（工具：`tools/lifecycle_fairness.py`，代码：`src/sqcad/lifecycle_bench/{audit,unseen,independent_ref,baselines}.py`，审计结果落 `remote_results/lifecycle_audit/`）：

| # | 防线 | 通道 | 工具/子命令 | 判据（预注册） |
|---|---|---|---|---|
| R1 | 元数据捷径审计 | 评价不可被猜 | `shortcut` | metadata(family,variant) 预测力 vs text-only 预测力分栏报告；观测等价对给出**任何 public-trace 策略的 oracle 一致率上界 = $1 - \frac{15}{1380} = 0.9891$**；发布层提供去元数据视图（`release`） |
| R2 | 标签敏感性审计 | 真值可独立检验 | `sensitivity` | 15 组 frozen 常数扰动（§7.7 表）重算全部 1380 标签：翻转率 <5% 稳健 / 5–30% 敏感区如实报告 / >30% 脆弱需重定位 |
| R3 | 未见机制 holdout | 泛化可被挑战 | `unseen` | 结构 knob（槽位移/拥挤填充数/未见实体/难度/存储/翻转槽）全新 seed 重建 660 episodes：oracle 与设计意图一致率 ≥0.9 且无反向翻转 → 机制可迁移；否则如实报告该 knob 为机制边界。**hitchhiker_pair 家族以配对翻转确认率为主判据**（`pair_flip_confirmed`：base 侧 archive $\wedge$ flip 侧 keep，同一公开 trace 上两侧同时成立）——flip 侧与 base label 相反是配对设计意图，单侧一致率无意义 |
| R4 | 基线必胜区预注册 | 评价不可被猜 | `matrix` | §7.7 表逐 bucket 预注册各基线必胜/必败区；运行后对照——预注册失准处即发现 |
| R5 | 独立实现一致性 | 真值可独立检验 | `independent` | `independent_ref.py` 第二实现（仅凭文档规则重写，不同结构）对全部 1380 episodes 与 `world.simulate_branch` 逐 slot 位级一致：cert status/workspace/adopted/probes/restore/success/penalties/成本/效用 |
| R6 | 人类锚定 κ | 真值可独立检验 | `human_anchor` + `score_anchor` | 分层抽样 30 case（盲化 family/variant，只给 public 文本），外部判官 keep/archive/neutral，Cohen's $\kappa$ 与 oracle：$\kappa \ge 0.6$ 人类可复现标签；输出协议 + 导出 + $\kappa$ 计算器，判官批次待外部执行 |
| R7 | 公开发布包 | 评价不可被猜 | `release` | `public_trace_only.jsonl`（去 family/variant/episode_id/regime）+ README + `tools/score_lifecycle_predictions.py` 官方打分器（纯查表：predictions → 选择分支的折扣价值/regret/一致率） |
| R8 | 预注册 + 迭代日志 | 全部 | 本文 + manifest | 审计协议、扰动集、knob 集、必胜区表全部先于运行写定；构建期设计调整成文（§7.8） |

**本批修订（20260817→0818，落地于代码与数据）**：
- R1 修订：public 层提供 `release` 去元数据视图；审计量化 metadata 捷径（预期接近 1.0——这正是分栏报告的原因）；
- R2 修订：`world.py` 成本常量改为从 `frozen.py` 导入（原 STORAGE_RATE/EXPOSURE_UNIT 硬编码 0.01/0.05，扰动审计需要可 patch 入口；行为零变化，合同测试 47 项通过）；
- **识别性修订（重要）**：harmful_stale/stable_negative 的纠错文本由 `"peanuts are no longer part of {e} diet"` 改为 `"{e}: peanuts -- the old peanut fact is wrong"`。原文本在无词干精确分词下与 stale 事实只共享实体 token（peanut$\ne$peanuts），词法 requalify 通道（≥REQUALIFY_OVERLAP）**从 public 层不可达**——识别只能靠 fid 链接，而 public.jsonl 不暴露 fid。新文本与 stale 事实共享 {entity, peanut}（=2，词法通道可达），与 good 事实只共享 entity（=1，m_good 保持 POSITIVE）；证书状态与 oracle 逐位不变（原依赖 fid 通道，现双通道同结果），行为等价（审计在 R5 独立实现 + 47 合同测试双重验证）。

### 7.7 预注册：扰动集 / knob 集 / 基线必胜区表

**R2 扰动集**（`audit.R2_PERTURBATIONS`，31 个 run；仅 HARM_PENALTY 进入场景设计需重建 episodes，其余复用缓存）：
GAMMA$\in\{0.7,0.95,0.99\}$；HARM_PENALTY$\in\{10,40\}$（重建）；STORAGE_RATE$\in\{0.005,0.02\}$；EXPOSURE_UNIT$\in\{0.02,0.1\}$；PROBE_COST$\in\{0.5,2\}$；TASK_VALUE$\in\{5,20\}$；TAU_TOL$\in\{0.2,1.0\}$；ADOPT_THRESHOLD$\in\{1,3\}$；PROBE_THRESHOLD$\in\{2,4\}$；WORKSPACE_BUDGET$\in\{8,12\}$；RECENCY_W$\in\{0.1,0.5\}$；FREQUENCY_W$\in\{0.05,0.2\}$；NEGATIVE_ATTENUATION$\in\{5,20\}$；REQUALIFY_OVERLAP$\in\{1,3\}$；PROBE_BUDGET_PER_TASK$\in\{0,2\}$。

**R3 knob 集**（`unseen.py`，15 bucket $\times$ 3 knob $\times$ 20 episodes = 900，预注册 seed 区 20260901+，全部未见实体池 16 名）：entity（未见实体）/ difficulty$\in\{1.5,2.0\}$ / slot_shift（rare_bridge·self_obscuring 救援任务位移至 3–5 或 6–9；hitchhiker pair 拥挤任务移槽 3/7 后翻转；其余家族退化为存储规模$\in\{6,12\}$）。

**R4 基线必胜区预注册表**（决策规则：sqcad_cert = 证书 NEGATIVE/MISMATCH→archive 否则 keep；sqcad_cert_conflict = + lineage_conflict→archive。√=期望动作正确，×=期望错——全部在运行前写定）：

| bucket | oracle | keep_all | archive_all | sqcad_cert | sqcad_cert_conflict | 说明 |
|---|---|---|---:|---:|---:|---:|---|
| hitchhiker/default | archive | × | √ | × | × | 关联唯一信号（Prop A 边界：观察等价对证明任何 observable 策略不可全对） |
| hitchhiker_pair base/flip | archive/keep | ×/√ | √/× | ×/√ | ×/√ | 对子：同 trace 反号 → oracle 一致率上界 |
| rare_bridge $\times$2 | keep | √ | × | √ | √ | T1/T2：keep 保护低频 bridge；archive 假遗忘 |
| version_update/update_before | archive | × | √ | × | √ | 冲突识别→archive；sqcad_cert 对 lineage-UNRESOLVED 保守 keep（框架修改候选点） |
| version_update/update_after | keep | √ | × | √ | √ | 早期任务需要旧版本；事件后 requalify |
| harmful_stale/correction_visible | archive | × | √ | √ | √ | 可见纠错→NEGATIVE→archive |
| harmful_stale/no_correction | neutral | ≈ | ≈ | ≈ | ≈ | 双分支同害 → 弃权奖励 |
| self_obscuring $\times$2 | keep | √ | × | √ | √ | crowding 中 probe 被严格竞争拒绝 |
| scope_mismatch/future_in_s1 | keep | √ | × | √ | √ | POSITIVE + 同 scope 未来 |
| scope_mismatch/future_in_s2 | archive | × | √ | × | × | 证书只看决策点局部；未来跨 scope 可观测但证书忽略（框架修改候选点；scope_literal 基线可赢） |
| stable_positive | keep | √ | × | √ | √ | 控制 |
| stable_negative | archive | × | √ | √ | √ | 控制；可见纠错→NEGATIVE |
| neutral | neutral | ≈ | ≈ | ≈ | ≈ | 控制；弃权 |

预注册统计声明（paired bootstrap，seed 20260817，2000 重采样）：sqcad_cert 在 mean lifecycle value 上**显著优于** keep_all（correction_visible + stable_negative 的 false-commit 差）；sqcad_cert 在 hitchhiker/base、update_before、future_in_s2 上**显著劣于** oracle 上界——如实报告；其余对子只报告 CI 不预注册方向。

### 7.8 迭代日志（R8：构建期设计调整成文）

| # | 日期 | 调整 | 原因与影响 | 证据 |
|---|---|---|---|---|
| 1 | 08-17 | correction_allergy fid 按家族映射（stable_negative→m1） | 原硬编码 m_stale 在控制家族指向不存在的事实；语义应为"可见纠错→NEGATIVE"；行为等价 | 合同测试 + 重建后 oracle 分布不变 |
| 2 | 08-17 | 测试层：PairedRollout 不可下标/RealizedSession 无 text | 测试 helper 适配 frozen dataclass 与扁平化消息 | 47 合同测试 |
| 3 | 08-17 | 金标泄漏检查改 token 级（`set(re.findall(...))`） | 子串匹配误报（"m1"$\in$"mem1"）；4MB 语料逐 fid 正则挂死 | 合同测试泄漏检查 |
| 4 | 08-18 | world.py 成本常量导入化（STORAGE_RATE/EXPOSURE_UNIT） | R2 扰动审计需要 patch 入口；行为零变化 | 47 合同测试全绿 |
| 5 | 08-18 | 纠错文本修订（§7.6） | 词法 requalify 通道从 public 层不可达（无词干精确分词）；fid 链接不暴露给消费者 | R5 独立实现一致 + 合同测试 |
| 6 | 08-18 | R3 pair slot 位移修复 | 翻转必须发生在 q_volunteer_crowd 查询所在槽（移动后翻转），否则验证失败 | unseen 测试 |
| 7 | 08-18 | R3 pair flip label 同步（判据修正） | `_flip_pair_slot` 只改 needed_fid 未同步 `decision_action_label`（MVP 中 flip 侧 label=keep），审计误报 20 个假 reversal；修复后翻转机制在所有移动槽位 100% 转移；配对确认改为主判据（§7.6 R3 行） | r3_unseen.json + 新增测试 |
| 8 | 08-18 | **R3 机制边界发现（如实报告，非 bug）** | (a) rescue 收益贴现位置敏感：rescue_possible 任务从 slot 2 移至 3–5/6–9 后 $\tau$ 0.68→{0.16,0.32,0.49–0.51}，oracle 由 keep 退化为 neutral（方向保持、强度跌破 TAU_TOL）——self_obscuring 0.0 / rare_bridge 0.35；(b) neutral 平衡对决策记忆存储大小敏感：storage 6/12 tokens 使 $\tau$ 0→{−0.65,−1.0} 偏 archive。二者均为机制边界而非标签错误（evaluator 为同一诚实反事实） | r3_unseen.json |

**预注册纪律**：上述审计协议、扰动集、knob 集、必胜区表在本节写定的同一批次完成，审计运行输出独立落盘（remote_results/lifecycle_audit/），报告如实回填——预注册失准处即发现，不调词。

## 8. 环 6：消融实验总表（必要性验证）

| # | 消融 | 要回答的问题 | 失败后的处理 | 落点 |
|---|---|---|---|---|
| 1 | 去 qualification | 作用域资格是否比关联性更重要 | 收缩 Permission 主张 | 环 4 no-qualification / 环 5 |
| 2 | scoped → global | 作用域限制是否减少负迁移 | 删除 transport gate | 环 5 scope_mismatch 族 |
| 3 | 去 competitive projection | 竞争性预算投影是否必要 | 回退简单衰减 | 环 5 crowding 族 |
| 4 | 去 protected focus | 低频高价值记忆是否得到保护 | 删除保护性主张 | 环 5 rare_bridge 族 |
| 5 | unresolved → default-cold | bounded fallback 是否避免过度保守 | 简化 fallback | 环 4 fallback 消融（E2 0.96→0.30、E3 0.86→0.42 已显著） |
| 6 | 去 negative attenuation | 负向资格是否减少 harmful retention | 降低负向治理强度 | 环 4 / 环 5 harmful_stale |
| 7 | 去 mismatch penalty | 作用域外过滤是否有独立贡献 | 不主张 scope filtering | 环 5 scope_mismatch |
| 8 | 去 restore/audit 路径 | 错误资格后能否恢复 | 简化工程接口 | 环 4 no_restore（F1 0.0455→0.0163 已证明非装饰） |
| 9 | probe 排序：VOI → BM25 | VOI 的价值信息收益 | 21- E2 验收 | 环 4/5 |
| 10 | crossing 强制 commit | 识别区间 gate 的必要性 | 21- E1 验收 | qualification_access tests |
| 11 | 去 lineage/version gate | 版本冲突识别必要性 | 16- version gate 负项已如实报告 | 环 4/5 |

（#1–#8 对应评审要求 §5 最低消融表；#9–#11 对应 `21-` §3 与 `22-` §9；各消融的证据引用见 §0 环→文档表。）

## 9. 环 7：统计协议与可复现性

- 统一合同：所有方法共享 history/candidate stream/reader/prompt/model/tool/retriever/top-k/workspace token/storage/evaluator/时间切分/随机种子（`01-`；原论文结果、proxy、本地复现分栏）；
- 显著性：studentized bootstrap CI（`bootstrap_ci.py`）+ LB 配对 bootstrap（`baselines.bootstrap_diff`，episode 级配对、固定 seed 20260817）；同种子同方法；
- 冻结：prompt、world version、seed、成本合同、budget、reader/evaluator 版本进入 manifest（`results/lifecycle_bench/manifest.json`）；测试集不调参（评审要求 §6 难点 B）；
- 公平性：LME-S 时间掩码、LoCoMo 冻结官方 QA 顺序、hidden 标签不出 public 层（leak 测试在合同测试中强制）；LB 六特征 + **R1–R8 公允性防线**（§7.6：元数据捷径审计、敏感性扰动、未见机制 holdout、必胜区预注册、独立实现一致性、人类锚 κ、发布包、迭代日志——判据与工具全部预注册）；
- 可复现命题：`reduction_closure.py` 16 项测试、`test_lifecycle_bench_contract.py` 47 项、`test_public_unified_contract.py` 23 项、`test_qualification_access.py` 5 项、`test_lifecycle_fairness.py` 22 项——全绿。具名基线扩展的每一行还需记录实现身份、commit/hash、传输公式、不可迁移组件及 seed；缺一项即降级为“未复现”。

## 10. 实验矩阵总表（状态追踪）

| 实验 | 数据 | 指标层 | 基线 | 状态 | 资产 |
|---|---|---|---|---|---|
| Gap 命题 A/B/C | LoCoMo/LME-S | L1 | 关联/衰减 | ✅ 12- | `01-`/`02-` |
| T1(a)(b)(c) | 受控机制世界 | L1 | W0/W1/W2/W3 | ✅ 13- | `reduction_closure.py` |
| T2 严格化 | 受控 | L1 | 忠实化约族 | ✅ 16- | 481f18c |
| P4 minimax 下界 | 受控 | L1 | 探测策略族 | ✅ 16- | 481f18c |
| 公开统一合同主表 | LME-S/LoCoMo | L2 | 见 §2.2 | ✅ 16-/19- | `public_unified_contract.py` |
| 闭源基线验证 | LME-S/LoCoMo | L2 | SimpleMem/Oblivion | ✅ 17- | 官方代码快照 |
| 半合成 overlay | LoCoMo 注入 | L2 | E1–E5 基线 | ✅ 18- | 18- 报告 |
| AutoDL GPU 复核 | LME-S/LoCoMo | L2 | 全基线 | ✅ 19- | autodl-tmp 快照 |
| 资格授权恢复 E1 | identification_recovery | L1 | 见 21- §3 | ⏳ 待办 | — |
| LB 数据集构建（含修订） | 规则世界 | — | — | ✅ 本批 | `results/lifecycle_bench/`（远端重建 hash 一致） |
| LB 公允性审计 R1–R5/R7 | SQCAD-LB | L3 | §7.6 判据 | ✅ 20-（R2 fragile 已重定位；R1/R3/R5 通过；远端复跑一致） | `remote_results/lifecycle_audit/` |
| LB 人类锚定 R6 | SQCAD-LB 30 case | L3 | $\kappa \ge 0.6$ | ⏳ 判官批次待外部（28 case 已导出） | `human_anchor` 导出 |
| LB 首轮基线矩阵 | SQCAD-LB | L3 | 见 §2.3/§7.7 | ✅ 20-（必胜区 13 行全命中；消融区分度在 no_censoring；框架修改 3 项判定） | `remote_results/lifecycle_audit/matrix.json` |
| LB 核心具名基线扩展 | SQCAD-LB | L3 | FadeMem / Oblivion / Memory Worth / DeMem / SimpleMem 的可迁移层 | ⏳ 按 §2.2 执行门：代理与官方复现分栏；ReMe 仅在 Phase B 进入 | 扩展 matrix + paired bootstrap |
| LB Phase B 端到端 | SQCAD-LB 子集 | L3 | 冻结 reader/LLM | ⏳ 待办（修改 3 scope 前瞻第一验证点） | — |
| 消融族 #1–#11 | 全部 | L1/L2/L3 | 逐项 | 部分 ✅ | 各报告 |

## 11. 交付物清单（评审要求 0808 §8 对照）

| 交付物 | 状态 |
|---|---|
| baseline/SOTA 复现表 | ✅ 本方案 §2 + `15-`/`17-` 审计（dense 行标注环境阻塞） |
| 数据集卡片 | ✅ `16-`（LME-S/LoCoMo）+ `22-`（SQCAD-LB，含标注效度说明） |
| 最小预实验报告 | ✅ `01-`/`02-`/`18-`（overlay 即评审要求 §3.3 快速验证） |
| 实验设计表 | ✅ 本文 §10 矩阵 + Go/No-Go（`22-` §10） |
| Score Qualification 方案 | ✅ `21-` + `qualification_access.py`（识别区间 $[L,U]$，阈值冻结） |
| Competitive Access Decay 方案 | ✅ `21-` §1.4（资格投影→排序投影，$B=10$ frozen） |
| 论文主张—证据对照表 | ✅ 本文 §12 声称纪律 |

## 12. 声称纪律与边界

- ✅ 有证据：T1/T2/P4 严格证明与数值；公开集治理策略最优 + 成本 Pareto；overlay fallback/probe/E5 显著；LB 六特征与 14 场景 oracle 一致；SimpleMem/Oblivion 官方级验证。
- ⚠️ 明确边界：公开数据 bm25 静态召回落后、version gate 负项、E2 harmful $n=10$ 不显著——如实报告，不调词。
- ❌ 不可声称：SQCAD 已优于 FadeMem/Oblivion/Memory Worth/DeMem/SimpleMem 等 SOTA（尚未完整端到端对比）；自建数据集代表真实 Agent Memory 场景（未完成标注效度与 Phase B）；相关性/LLM 解释为因果证据；token 减少必然提升智能/信噪比；公开集结果证明因果资格。
- 表述模板固定：**治理策略最优 + 成本 Pareto + 检索质量差距**；Guard 定位为"证据覆盖保底"，不升级为理论主张（`19-` §6、`21-` 结论摘要）。

---

## 附：本批（20260817–0818）执行摘要

1. SQCAD-LifecycleBench MVP 构建完成：1380 episodes、14 场景 bucket oracle==label 严格一致、15 观测等价对、三层序列化 + manifest 冻结（`results/lifecycle_bench/`）；
2. 合同测试 47 项全绿（含 6 特征、分支不变性、probe 计费/拒绝、split hygiene、金标泄漏检查）；
3. 修复一处真实器缺陷：stable_negative 控制的纠错事件 fid 硬编码 m_stale，改为按家族映射到 m1（保持语义为"可见纠错→NEGATIVE"；行为等价，policy log 证书字面量更新后重建数据集）；
4. 数据集修订（/goal 要求"缺的防线补充"）：R1–R8 公允性防线全套落地（§7.6）——`audit.py`（R1/R2）、`unseen.py`（R3）、`baselines.py`（R4/基线矩阵）、`independent_ref.py`（R5 第二实现）、`human_anchor`/`score_anchor`（R6）、`release`/`score_lifecycle_predictions.py`（R7）、§7.7/§7.8（R8 预注册 + 迭代日志）；纠错文本修订使词法 requalify 通道对 public 层可达（oracle 逐位不变）；
5. 全量测试通过（原 329 项 + 新增 `test_lifecycle_fairness.py` 22 项）。
